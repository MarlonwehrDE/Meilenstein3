<Page x:Class="Meilenstein3.GUI.FinanzmanagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Meilenstein3.GUI"
      mc:Ignorable="d"
      Title="FinanzmanagerPage" Height="450" Width="800">
    <Grid>
        <TextBox HorizontalAlignment="Center" FontSize="34" Height="50" VerticalAlignment="Top">Finanzmanager</TextBox>
        <ListView Margin="50" ItemsSource="{Binding Transaktionen}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Betrag">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text = "{Binding Betrag}" Foreground="{Binding Color}" IsReadOnly="True"></TextBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header = "Beschreibung">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text = "{Binding Beschreibung}" IsReadOnly="True"></TextBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Datum">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text = "{Binding Datum}" IsReadOnly="True"></TextBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox HorizontalAlignment="Right" VerticalAlignment="Bottom" Text = "{Binding Kontostand,StringFormat= Kontostand : {O:2F}}" Foreground="{Binding Color}"></TextBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        
        <Button VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="50,25" Click = "TransaktionenHinzufuegen_Click">Transaktion Hinzufügen</Button>
        
        
    </Grid>
    
</Page>
